# library path
add_lib <- 'C:\\Users\\mbeck\\R\\library'
if(file.exists(add_lib)) .libPaths(add_lib)
rm('add_lib')

# startup message
cat('SWMP workshop training materials...\n')

# packages to use
library(knitr)
